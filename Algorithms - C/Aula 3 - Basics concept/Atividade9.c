// 9. Desenvolva para somar dois números 
// quaisquer e apresentar o resultado na tela.
#include<stdio.h>

int main()
{
    int x, y, soma;
    x = 5;
    y = 7;

    soma = (x + y);

    printf("%d", soma);
    return 0;
}